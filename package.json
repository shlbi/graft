{
  "name": "graft",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "typescript": "5.8.3"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "npm run build && node scripts/test.mjs",
    "demo": "npm run build && node demo/verify.mjs",
    "ui": "npm run build && node ui/server.mjs"
  }
}
